<template>
  <header class="border-b px-6 py-4 transition-[background-color,border-color] duration-400" style="background-color: var(--bg-header); border-color: var(--border)">
    <div class="max-w-350 mx-auto flex items-center">
      <span class="text-xl font-bold tracking-tight">
        Ani<span style="color: var(--accent)" class="transition-colors duration-400">Binge</span>
      </span>
    </div>
  </header>
</template>

<script setup lang="ts"></script>
